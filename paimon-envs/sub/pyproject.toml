[project]
name = "simulation_util"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ase>=3.25.0",
]

[tool.setuptools.packages.find]
include = ["simulation_util*"]
